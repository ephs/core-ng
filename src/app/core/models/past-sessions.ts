export interface PastSessions {
  category: string;
  name: string;
  date: string;
  startDate: string;
  endDate: string;
  startTime: string;
  endTime: string;
  firstName: string;
  lastName: string;
  attended: string; //Core keeps it as 'yes' or 'no' for some reason?
}
